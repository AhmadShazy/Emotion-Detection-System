[pytest]
testpaths = tests
markers =
    live: hits the real app and loads all four ML models (slow, ~40s startup)
# Quiet the noisy model-loading warnings so test failures stay readable.
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
