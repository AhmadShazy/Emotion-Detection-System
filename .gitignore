# Data
data/
# Environment
.venv/
.venv_broken/
__pycache__/

# External Models
external/whisper/
external/ser_project/
external/openface/
wav2vec2_checkpoints/
external/speechbrain/
external/faster_whisper/
external/mediapipe/

# IDE & Editor
.vscode/
.idea/
# Local run configuration. The interpreter path in launch.json is whatever this
# machine happens to use, so committing it would hand a teammate a broken one.
.claude/

# Environment configurations
.env
.env.*
# ...but the template must be committed, or a fresh clone has no idea what
# settings exist. It contains placeholders only, never real values.
!.env.example

# Logs
*.log